==============
Report manager
==============

The report manager allows administrators to create custom reports. Administrators will require 
to be familiar with the SQL language and the frePPLe database schema.

Custom reports can optionally be shared with other users. 
