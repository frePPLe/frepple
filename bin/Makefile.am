#
# Process this file with automake to produce Makefile.in
#

EXTRA_DIST = test.xml

nobasedir = $(bindir)
nobase_DATA = frepple.xsd frepple_core.xsd init.xml

clean-local:
	rm -rf *.tds *.exe *.lib *.dll *.so *.exp *.tds *.BAK *.ilk *.out output*.* frepple
